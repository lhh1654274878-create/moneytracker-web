# iPhone 使用完整指南

## 📱 从 Windows 到 iPhone 完整流程

### 准备工作

**你需要**：
- ✅ Windows 台式机
- ✅ iPhone 手机
- ✅ 同一个 WiFi 网络

**不需要**：
- ❌ Mac 电脑
- ❌ 云服务器
- ❌ 付费服务

---

## 🚀 三步完成部署

### 第一步：启动本地服务器（在 Windows 上）

#### 方法 A：使用 Python（推荐）

1. **检查是否已安装 Python**：
   - 按 `Win + R`，输入 `cmd`，回车
   - 输入 `python --version` 或 `python3 --version`
   - 如果显示版本号（如 `Python 3.9.0`），说明已安装

2. **如果没有 Python**：
   - 访问 [python.org](https://www.python.org/downloads/)
   - 下载 Windows 安装包
   - 安装时**勾选 "Add Python to PATH"**

3. **启动服务器**：
   ```bash
   # 打开 CMD 或 PowerShell
   cd E:\Workbuddy Works\MoneyTracker-Web
   python -m http.server 8080
   ```

4. **看到提示**：
   ```
   Serving HTTP on 0.0.0.0 port 8080 (http://0.0.0.0:8080/) ...
   ```
   说明服务器已启动 ✅

#### 方法 B：使用 Node.js

如果你有 Node.js：

```bash
# 全局安装 http-server（仅首次需要）
npm install -g http-server

# 启动服务器
cd E:\Workbuddy Works\MoneyTracker-Web
http-server -p 8080
```

#### 方法 C：使用 Chrome 浏览器扩展

1. Chrome 浏览器搜索扩展：**"Web Server for Chrome"**
2. 安装后，选择 `MoneyTracker-Web` 文件夹
3. 点击启动，记住端口号（默认 8887）

---

### 第二步：查找电脑 IP 地址

#### Windows 操作

1. 按 `Win + R`，输入 `cmd`，回车
2. 输入命令：
   ```bash
   ipconfig
   ```
3. 找到 **"无线局域网适配器 WLAN"** 或 **"以太网适配器"**
4. 找到 **IPv4 地址**，例如：
   ```
   IPv4 地址 . . . . . . . . : 192.168.1.100
   ```
5. **记住这个 IP 地址**（如 `192.168.1.100`）

#### 常见 IP 地址格式

- `192.168.0.xxx`
- `192.168.1.xxx`
- `10.0.0.xxx`

**注意**：
- ❌ 不是 `127.0.0.1`（这是本机地址）
- ❌ 不是 `169.254.x.x`（这是未连接网络）

---

### 第三步：在 iPhone 上访问

#### 1. 确保连接同一 WiFi

- iPhone 打开 **设置 → WiFi**
- 确认连接的 WiFi 名称与电脑相同

#### 2. 打开应用

1. iPhone 打开 **Safari 浏览器**
2. 在地址栏输入：
   ```
   http://你的IP地址:8080
   ```
   例如：`http://192.168.1.100:8080`

3. 如果一切正常，会看到钱迹应用首页 ✅

#### 3. 添加到主屏幕（关键步骤）

1. 点击 Safari 底部的 **分享按钮** 📤（中间的方框和箭头图标）
2. 向下滚动菜单
3. 找到 **"添加到主屏幕"** 或 **"Add to Home Screen"**
4. 修改名称（默认 "钱迹"）或直接点击 **"添加"**
5. 返回主屏幕，看到钱迹图标 ✅

#### 4. 开始使用

- 点击主屏幕的钱迹图标
- 应用全屏打开（无浏览器地址栏）
- **现在可以关闭电脑，应用已安装完成**

---

## 💡 使用技巧

### 离线使用

**好消息**：添加到主屏幕后，应用支持离线使用！

- ✅ 断开 WiFi 可以继续记账
- ✅ 关闭电脑也能使用
- ✅ 数据存储在 iPhone 本地

### 快速记账流程

1. 打开应用
2. 点击 **"记支出"** 或 **"记收入"**
3. 输入金额（如 `50`）
4. 选择分类（如 **餐饮** 🍜）
5. 可选添加备注（如 "午餐"）
6. 点击 **"保存"**

⏱️ **总耗时：3-5 秒**

### 查看统计

- **首页余额卡片**：实时显示总余额、本月收支
- **明细页**：查看所有交易记录
- **删除记录**：点击任意记录，确认删除

---

## 🔧 故障排查

### ❌ 问题 1：iPhone 无法访问 `http://192.168.x.x:8080`

**可能原因**：

1. **不在同一 WiFi**
   - 解决：确保电脑和 iPhone 连接相同 WiFi

2. **IP 地址错误**
   - 解决：重新在电脑上运行 `ipconfig` 确认

3. **端口号错误**
   - 解决：检查 Python 启动时显示的端口号

4. **防火墙拦截**
   - 解决（Windows）：
     - 控制面板 → Windows Defender 防火墙
     - 高级设置 → 入站规则 → 新建规则
     - 端口规则 → TCP → 特定端口 `8080` → 允许连接

5. **服务器未启动**
   - 解决：检查 CMD 窗口是否还在运行，没有报错

---

### ❌ 问题 2：添加到主屏幕后打不开

**解决**：

1. 删除主屏幕图标
2. Safari 重新访问 `http://IP地址:8080`
3. 再次添加到主屏幕
4. 确保第一次打开时电脑服务器还在运行

---

### ❌ 问题 3：数据丢失了

**原因**：
- 清除了 Safari 浏览器缓存
- 删除了主屏幕图标

**预防**：
- 定期截图保存重要数据
- 不要清除浏览器全部数据

---

### ❌ 问题 4：电脑关机后应用打不开

**正常现象**：首次启动需要电脑在线

**解决**：
1. 第一次添加到主屏幕后，先打开应用一次
2. 确保缓存成功
3. 之后即可离线使用

如果仍然无法离线：
- 检查 Safari 设置 → 隐私与安全性
- 确保未开启 **"阻止所有 Cookie"**

---

## 🎯 高级技巧

### 永久访问（无需每次启动电脑）

如果希望应用永久在线，可以部署到免费云服务：

#### GitHub Pages（推荐）

1. 注册 [GitHub](https://github.com)
2. 创建新仓库（Repository）
3. 上传 `MoneyTracker-Web` 文件夹内所有文件
4. 仓库设置 → Pages → 选择 `main` 分支
5. 等待部署（约 1 分钟）
6. 获得网址：`https://你的用户名.github.io/仓库名/`
7. iPhone 访问该网址，添加到主屏幕

**优势**：
- ✅ 永久免费
- ✅ 全球访问
- ✅ 无需自己维护服务器

---

## 📊 完整流程时间表

| 步骤 | 耗时 | 一次性/重复 |
|------|------|-------------|
| 安装 Python | 5 分钟 | 一次性 ✅ |
| 启动服务器 | 30 秒 | 每次使用 |
| 查找 IP 地址 | 1 分钟 | 一次性 ✅ |
| iPhone 访问 | 30 秒 | 一次性 ✅ |
| 添加到主屏幕 | 30 秒 | 一次性 ✅ |
| **首次总计** | **~8 分钟** | - |
| **后续使用** | **0 秒** | 直接打开 |

---

## 📱 使用效果预览

### 首页

```
┌─────────────────────────────────┐
│          钱迹                    │
├─────────────────────────────────┤
│  ┌───────────────────────────┐  │
│  │    当前余额                │  │
│  │    ¥ 8,000.00             │  │
│  │                            │  │
│  │  本月收入    本月支出      │  │
│  │  ¥8,000     ¥31.00        │  │
│  └───────────────────────────┘  │
│                                  │
│  ┌──────────┐  ┌──────────┐    │
│  │ 📈 记收入│  │ 📉 记支出│    │
│  └──────────┘  └──────────┘    │
│                                  │
│  最近交易                        │
│  🍜 餐饮    早餐    -¥25.00     │
│  🚗 交通    地铁    -¥6.00      │
└─────────────────────────────────┘
```

### 添加记录

```
┌─────────────────────────────────┐
│  记一笔                   ×      │
├─────────────────────────────────┤
│  ┌──────────┬──────────┐       │
│  │   支出   │   收入   │       │
│  └──────────┴──────────┘       │
│                                  │
│         金额                     │
│        50.00                     │
│                                  │
│  分类                            │
│  🍜餐饮 🚗交通 🛍️购物 🎮娱乐   │
│  💊医疗 🏠住房 📚教育 📱通讯   │
│                                  │
│  备注（选填）                    │
│  ┌────────────────────────┐    │
│  │ 午餐                    │    │
│  └────────────────────────┘    │
│                                  │
│  ┌────────────────────────┐    │
│  │         保存            │    │
│  └────────────────────────┘    │
└─────────────────────────────────┘
```

---

## 📞 需要帮助？

### 常见问题优先级

1. **启动服务器失败** → 检查 Python 是否安装
2. **iPhone 访问失败** → 检查 IP 地址和 WiFi
3. **添加后打不开** → 首次需保持服务器在线
4. **数据丢失** → 不要清除浏览器缓存

### 检查清单

在求助前，请确认：
- [ ] 电脑和 iPhone 在同一 WiFi
- [ ] CMD 窗口显示服务器正在运行
- [ ] IP 地址正确（不是 127.0.0.1）
- [ ] Safari 地址栏输入完整（包含 `http://` 和端口号）

---

**祝使用愉快！如有问题，随时提供错误截图寻求帮助。**
